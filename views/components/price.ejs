<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gold Rate - Goldverse</title>
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(to right, #f6f8fc, #fdfcfb);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            max-width: 500px;
            width: 90%;
            background-color: #fff;
            padding: 2rem;
            border-radius: 20px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            transition: 0.3s ease-in-out;
            animation: fadeIn 0.6s ease;
        }

        h1 {
            text-align: center;
            font-size: 1.8rem;
            margin-bottom: 1rem;
            color: #333;
        }

        .price-info {
            margin: 1rem 0;
            font-size: 1.1rem;
        }

        .price-info span {
            font-weight: 600;
            color: #444;
        }

        .highlight {
            font-size: 2rem;
            font-weight: bold;
            color: #ff9900;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0px);
            }
        }

        .footer {
            text-align: center;
            font-size: 0.9rem;
            color: #777;
            margin-top: 2rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📈 Today's Gold Rate</h1>

        <div class="price-info">
            <span>Per Gram (INR):</span> <div class="highlight"><%= pricePerGram %></div>
        </div>

        <div class="price-info">
            <span>Per Ounce (INR):</span> <%= data.price %>
        </div>

        <div class="price-info">
            <span>Currency:</span> <%= data.currency %>
        </div>

        <div class="price-info">
            <span>Last Updated:</span> <%= readableDate %>
        </div>
        
        <div class="price-info">
            <span>metal:</span> <%= data.metal%>
        </div>

        <div class="footer">
            Powered by Goldverse ✨
        </div>
    </div>

    <script>
        // Optional animation or dynamic reload in future
        console.log("Gold rate page loaded 🚀");
    </script>
</body>
</html>
